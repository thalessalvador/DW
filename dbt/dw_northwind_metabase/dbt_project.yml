name: dw_northwind_metabase
version: 1.0.0
config-version: 2

profile: dw_northwind_metabase

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
packages-install-path: "packages"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dw_northwind_metabase:
    staging:
      +materialized: view
      +schema: stg
    intermediate:
      +materialized: view
      +schema: stg
    dw:
      +materialized: table
      +schema: dw
    marts:
      dm_sales:
        +materialized: view
        +schema: dm_sales
      dm_b2b:
        +materialized: view
        +schema: dm_b2b
      dm_b2c:
        +materialized: view
        +schema: dm_b2c
      dm_saas:
        +materialized: view
        +schema: dm_saas
      dm_product_analytics:
        +materialized: view
        +schema: dm_product_analytics

snapshots:
  dw_northwind_metabase:
    +schema: snapshots
